<source>
  @type  forward
  port  24224
</source>
<match docker.*>
  @type color_stripper
  strip_fields log
</match>
<filter docker.*>
  @type record_transformer
  enable_ruby
  <record>
    message ${log}
    severity ${source == 'stderr' ? 'error' : 'debug'}
  </record>
  remove_keys source,log,container_id
</filter>
<match **>
  type copy
  <store>
    @type file
    path /fluentd/log/docker.*.log
    symlink_path /fluentd/log/docker.log
    append true
    time_slice_format %Y%m%d
    time_slice_wait 10m
    compress gzip
    utc
  </store>
  <store>
    type google_cloud
    flush_interval 5
  </store>
</match>